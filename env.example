# Environment variables for Landing Page Template

# Admin credentials (change these in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password123

# Session secret (change this in production - use a strong random string)
SESSION_SECRET=change-this-in-production-use-a-strong-random-string

# Resend email service configuration
RESEND_API_KEY=re_1234567890abcdef

# Database configuration (PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Site configuration
SITE_URL=https://your-domain.vercel.app

# Admin email addresses (update these with your actual admin emails)
# General admins receive ALL notifications (both player and non-player submissions)
ADMIN_EMAIL_1=admin1@yourdomain.com
ADMIN_EMAIL_2=admin2@yourdomain.com
ADMIN_EMAIL_3=admin3@yourdomain.com
ADMIN_EMAIL_4=admin4@yourdomain.com

# Player-only admin emails (these admins ONLY receive player submission notifications)
# Leave blank if not needed
ADMIN_EMAIL_PLAYER_1=
ADMIN_EMAIL_PLAYER_2=
ADMIN_EMAIL_PLAYER_3=
ADMIN_EMAIL_PLAYER_4=

# Non-player-only admin emails (these admins ONLY receive non-player submission notifications)
# Leave blank if not needed
ADMIN_EMAIL_NON_PLAYER_1=
ADMIN_EMAIL_NON_PLAYER_2=
ADMIN_EMAIL_NON_PLAYER_3=
ADMIN_EMAIL_NON_PLAYER_4=

# Email configuration
# For development/testing, you can use Resend's test domain: onboarding@resend.dev
# For production, use a verified domain (add and verify at https://resend.com/domains)
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=Your Company Name
